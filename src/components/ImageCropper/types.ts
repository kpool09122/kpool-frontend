export type ImageCropperDictionary = {
  title: string;
  description: string;
  confirm: string;
  cancel: string;
  reselect: string;
  loading: string;
  loadFailed: string;
  cropFailed: string;
};
